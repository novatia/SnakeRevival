#pragma once
#define DISPLAY_WIDTH 80
#define DISPLAY_HEIGHT 25
#define COLOR_HEADER 9
#define TICKS_PER_FRAME 0.16f

namespace SnakeRevival {
	typedef enum Colors { None, Red, Green, Blue, Yellow, Purple, White };
	typedef enum Alignments { Left, Right, Center, Top, Bottom};
}